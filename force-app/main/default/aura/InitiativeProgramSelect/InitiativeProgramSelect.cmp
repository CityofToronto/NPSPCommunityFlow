<aura:component implements="lightning:availableForFlowScreens" access="global" controller="InitiativeProgramController">
    <aura:attribute name="initiatives" type="List" />  
    <aura:attribute name="programs" type="List" />    
    <aura:handler name="init" value="{!this}" action="{!c.init}" />

    <lightning:select name="initiative" label="Initiative" aura:id="initiativeSelect" required="true" class="slds-size_1-of-2" onchange="{! c.onChange }">
        <aura:iteration items="{!v.initiatives}" var="option">
            <option text="{!option.Name}" value="{!option.Id}" />
        </aura:iteration>
    </lightning:select>

    <lightning:select name="program" label="Program" aura:id="programSelect" required="true" class="slds-size_1-of-2">
        <aura:iteration items="{!v.programs}" var="option">
            <option text="{!option.Name}" value="{!option.Id}" />
        </aura:iteration>
    </lightning:select>

</aura:component>	
